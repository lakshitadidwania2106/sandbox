langchain
langchain-anthropic
fastapi
uvicorn
python-dotenv
faiss-cpu
sqlite-utils
pydantic
# Security Layer Dependencies
# ============================
# These are the dependencies required for the security package
# (Lakera Guard + Presidio integration).

# --- Layer 3: Lakera Guard ---
requests>=2.28.0          # HTTP client for Lakera Guard API calls

# --- Layer 4: Presidio ---
presidio-analyzer>=2.2.0  # Microsoft Presidio - PII entity detection engine
presidio-anonymizer>=2.2.0 # Microsoft Presidio - PII redaction/anonymization engine
spacy>=3.5.0              # NLP backend required by Presidio's default recognizers

# --- Shared ---
python-dotenv>=1.0.0      # Load environment variables from .env file

